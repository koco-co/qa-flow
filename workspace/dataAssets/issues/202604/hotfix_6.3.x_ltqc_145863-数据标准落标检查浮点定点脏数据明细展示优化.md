---
suite_name: "Hotfix 用例 - 数据标准落标检查浮点数/定点数类型检查项结果展示优化"
description: "验证 Bug #145863 修复效果"
tags:
  - hotfix
  - bug-145863
create_at: "2026-04-16"
status: 草稿
origin: zentao
---

## 数据标准

### 落标检查

#### 检查项脏数据明细展示

##### 【145863】验证落标检查脏数据明细中浮点数/定点数字段以完整精度格式展示，不出现科学计数法

> 前置条件

```
1、环境要求：岚图环境已部署 dt-center-metadata 的 hotfix_6.3.x_ltqc_145863 修复版本；测试账号可访问【数据标准】相关菜单。

2、测试表已在 tytest 库下创建完成，建表语句如下（若未建表请先执行）：

DROP TABLE IF EXISTS tytest.ltqc_standard_check_145863;
CREATE TABLE tytest.ltqc_standard_check_145863 (
  id          BIGINT,
  col_float   FLOAT,
  col_double  DOUBLE,
  col_decimal DECIMAL(20, 10),
  col_int     INT,
  col_null    DOUBLE
) STORED AS PARQUET;

INSERT INTO TABLE tytest.ltqc_standard_check_145863
SELECT 1, CAST(123456789012.34567 AS FLOAT),  CAST(123456789012.34567 AS DOUBLE),  CAST(1234567890.1234567890 AS DECIMAL(20,10)), 100, NULL
UNION ALL
SELECT 2, CAST(0.000000123456 AS FLOAT),      CAST(0.000000123456 AS DOUBLE),      CAST(0.0000001234 AS DECIMAL(20,10)),            200, NULL
UNION ALL
SELECT 3, CAST(1.5 AS FLOAT),                 CAST(1.5 AS DOUBLE),                 CAST(1.5000000000 AS DECIMAL(20,10)),            300, 1.5;

3、账号具备【数据标准】模块的新建标准、发布标准、落标检查新建与查看权限。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| **一、新建数据标准（用于落标检查的检查项）** | | |
| 1 | 顶部导航点击【数据标准】→ 左侧菜单展开【标准管理】→ 点击【标准定义】 | 进入标准定义列表页 |
| 2 | 点击左侧标准目录树中任意一个目录节点（若无目录，先新建一个目录），记下目录名称备用；点击右上角【新建标准】 | 进入新建标准页面，左侧为「业务属性」表单，右侧为「技术属性」表单 |
| 3 | 填写左侧「业务属性」：<br>① 【中文名称】输入：`测试整数检查标准_145863`<br>② 【英文名称】输入：`test_int_standard_145863`<br>③ 【英文缩写】输入：`tisst145863`<br>④ 【标准目录】选择步骤 2 中记下的目录<br>⑤ 其余字段（业务定义、标准来源、自定义属性）留空 | 中文名称、英文名称、英文缩写、标准目录四项填写完成，必填项无红色校验提示 |
| 4 | 填写右侧「技术属性」，按以下顺序填写：<br>① 【数据类型】下拉选择：`数值型`<br>② 【数据长度】留空（不限制）<br>③ 【数据精度】留空（不限制）<br>④ 【是否允许空值】选择：`可空`<br>⑤ 【是否允许重复】选择：`可重复`<br>⑥ 【默认值】留空<br>⑦ 【取值范围】配置：前运算符选 `>`，前值输入 `999`（含义：col_int 必须 > 999；测试数据 100/200/300 均不满足，全部为脏数据）<br>⑧ 【精度倍数】【偏移量】留空<br>⑨ 【枚举范围】留空 | 技术属性填写完成；取值范围显示为「> 999」 |
| 5 | 点击右上角【上线】（或【发布】）按钮 | 弹出确认提示；确认后标准状态变为「已上线」，列表中可见该标准 |
| **二、新建落标检查任务** | | |
| 6 | 左侧菜单点击【标准管理】→ 点击【落标检查】 | 进入落标检查任务列表页 |
| 7 | 点击【新建任务】，弹出新建任务向导（共 3 步），当前为第 1 步「检查数据范围」 | 页面显示三步进度条：检查数据范围 / 检查字段列表 / 检查周期 |
| 8 | 第 1 步 - 填写「检查数据范围」：<br>① 【数据源】下拉选择已接入的 SparkThrift 数据源（tytest 所在数据源）<br>② 【数据库】下拉选择 `tytest`<br>③ 【数据表】下拉选择 `ltqc_standard_check_145863`<br>④ 分区字段留空（该表无分区）<br>⑤ 【对标标准 > 标准目录】点击下拉，选择步骤 2 中存放 `测试整数检查标准_145863` 的目录<br>⑥ 点击右下角【下一步】 | 表单校验通过，进入第 2 步「检查字段列表」；左侧展示表的所有字段（id / col_float / col_double / col_decimal / col_int / col_null） |
| 9 | 第 2 步 - 配置「检查字段列表」：<br>① 找到字段 `col_int` 所在行<br>② 将该行右侧【是否开启检查】开关拨为「开启」（蓝色）<br>③ 在 `col_int` 行的【检查项】列点击下拉，选择步骤 3-5 中新建的标准 `测试整数检查标准_145863`<br>④ 点击右下角【临时检查】（立即单次执行，无需配置周期） | 提示「临时检查任务创建成功」；页面返回落标检查任务列表 |
| **三、查看检查结果与脏数据明细** | | |
| 10 | 在落标检查任务列表中找到 `ltqc_standard_check_145863` 对应任务行，等待【检查状态】变为「已完成」（可手动刷新页面） | 任务状态显示「已完成」；【不达标字段数/检查失败数】列显示 `1/0`（col_int 不达标） |
| 11 | 点击该任务行操作列中的【查看检查结果】 | 跳转到检查结果明细列表，展示 col_int 字段的检查记录；「是否达标」列显示「未达标」 |
| 12 | 找到 `col_int` 字段、「未达标」的记录行，点击操作列【查看详情】 | 右侧滑出「col_int - 不达标数据详情」抽屉；表格中显示不达标行的所有字段，列包含：id / col_float / col_double / col_decimal / col_int / col_null，共 3 行数据 |
| **四、验证浮点数/定点数展示精度** | | |
| 13 | 核对 id=1 行的 `col_double` 值 | 显示为完整小数（如 `123456789012.34568`），**不出现科学计数法**（不含字母 `E`） |
| 14 | 核对 id=2 行的 `col_double` 值 | 显示为 `1.23456E-7` 对应的完整格式（`0.000000123456` 或精度等效），**不出现** `E` |
| 15 | 核对 id=1 行的 `col_decimal` 值 | 显示为 `1234567890.1234567890`，10 位小数完整，不截断、不丢精度 |
| 16 | 核对 id=2 行的 `col_decimal` 值 | 显示为 `0.0000001234`，精度完整，**不出现科学计数法** |
| 17 | 核对 id=3 行的 `col_float`、`col_double`、`col_decimal` 值 | 分别显示为 `1.5`、`1.5`、`1.5000000000`，普通小数正常，无异常格式 |
| 18 | 核对所有行的 `col_int` 值 | 分别显示 `100`、`200`、`300`，整数类型不受影响，无多余小数点或科学计数法 |
| 19 | 核对 id=1、id=2 行的 `col_null` 值 | 显示为空（无内容），不报错；id=3 行显示为 `1.5` |
