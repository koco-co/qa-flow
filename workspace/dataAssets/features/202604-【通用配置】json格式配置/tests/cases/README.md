# Cases — 【通用配置】json格式配置

case 编号到业务场景的映射表（§4.5 L3）。

| 编号 | 优先级 | 业务场景 |
|------|--------|----------|
| t01 | P0 | 验证新增key完整正向流程（含正则测试） |
| t02 | P1 | 验证新增key时key字段为空不可提交 |
| t03 | P1 | 验证新增key时key字段输入超255字符不可提交 |
| t04 | P1 | 验证新增key时key字段输入恰好255字符边界值可成功提交 |
| t05 | P2 | 验证新增key时数据源类型三种选项可正常切换 |
| t06 | P1 | 验证新增key表单中切换数据源类型后其余字段内容仍保留 |
| t07 | P1 | 验证编辑key名称、value格式、数据源类型并保存生效 |
| t08 | P1 | 验证新增子层级完整流程 |
| t09 | P1 | 验证第5层级的新增子层级按钮是置灰、不可点击 |
| t10 | P1 | 验证单个删除含子层级的key会联动删除子层级数据 |
| t11 | P1 | 验证批量删除多条key（含子层级） |
| t12 | P1 | 验证key名模糊搜索功能（含子层级key命中） |
| t13 | P1 | 验证数据源类型筛选功能 |
| t14 | P1 | 验证5层层级展开下钻及展开图标显示逻辑 |
| t15 | P1 | 验证value格式有内容时正则测试控件显示及匹配通过失败场景 |
| t16 | P0 | 验证导入正确文件全流程（重复则跳过） |
| t17 | P1 | 验证导入模板下载功能 |
| t18 | P1 | 验证重复处理规则「重复则覆盖更新」生效 |
| t19 | P1 | 验证重复处理规则「重复则跳过」对已存在key不覆盖 |
| t20 | P1 | 验证导入文件key名超255字符时标红并批注长度超限 |
| t21 | P1 | 验证导入文件必填项未填写时标红并批注必填项未填写 |
| t22 | P1 | 验证导入文件二层key上一层级key名无法匹配时标红并批注提示 |
| t23 | P1 | 验证导入非xlsx格式文件时报错 |
| t24 | P1 | 验证导入功能正常（重复则覆盖更新，1~5层key存在相同 -> 报错） |
| t25 | P1 | 验证导入功能正常（重复则覆盖更新，1层key已存在 -> 更新1层key） |
| t26 | P1 | 验证导入功能正常（重复则覆盖更新，1层key不存在 -> 新增1层key） |
| t27 | P1 | 验证导入功能正常（重复则覆盖更新，2~5层上一层key存在+key存在+value不存在 -> 更新N层value） |
| t28 | P1 | 验证导入功能正常（重复则覆盖更新，2~5层上一层key存在+key不存在 -> 新增N层key） |
| t29 | P1 | 验证导入功能正常（重复则覆盖更新，2~5层上一层key不存在 -> 报错） |
| t30 | P1 | 验证导入功能正常（重复则跳过，1~5层key存在相同 -> 报错） |
| t31 | P1 | 验证导入功能正常（重复则跳过，1层key已存在 -> 跳过不变） |
| t32 | P1 | 验证导入功能正常（重复则跳过，1层key不存在 -> 新增1层key） |
| t33 | P1 | 验证导入功能正常（重复则跳过，2~5层上一层key存在+key存在+value不存在 -> 跳过不变） |
| t34 | P1 | 验证导入功能正常（重复则跳过，2~5层上一层key存在+key不存在 -> 新增N层key） |
| t35 | P1 | 验证导入功能正常（重复则跳过，2~5层上一层key不存在 -> 报错） |
| t36 | P0 | 验证导出列表数据完整流程及文件命名 |
| t37 | P1 | 验证筛选后导出仅包含筛选结果数据 |
| t38 | P2 | 验证搜索无结果时的空状态展示 |
| t39 | P2 | 验证key数量达1000条以上时列表加载和搜索性能正常 |
| t40 | P1 | 验证大数据量场景key记录下载数量是否存在限制 |
| t41 | P1 | 验证删除已被完整性和有效性校验规则引用的key后规则不受影响 |
| t42 | P1 | 验证编辑弹窗与新增弹窗保持一致 |
| t43 | P1 | 验证导入失败时仅出现单个错误通知 |
| t44 | P1 | 验证筛选导出三种筛选组合的行为一致性 |
| t45 | P1 | 验证编辑页与新增页表单结构保持一致 |
| t46 | P1 | 验证导入xlsx第2个sheet页第一层级key名不存在时标红列应为「第一层级key名」而非「key」 |
