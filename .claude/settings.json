{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'ROOT=$(git rev-parse --show-toplevel 2>/dev/null) && cd \"$ROOT\" && git diff --quiet && git diff --cached --quiet || (git add -A && git commit -m \"chore: auto-commit session changes\" && echo \"[stop-hook] 已自动提交\") || true'"
          }
        ]
      }
    ]
  }
}
