# R1 review — Probe 后

## 检查清单
- [ ] probe 结果是否覆盖了 PRD 中所有关键页面/API？
- [ ] 是否有遗漏的模块或功能点？
- [ ] enhanced.md Appendix A 中的页面要点是否完整？
- [ ] 断言是否严格遵循 assertion_fidelity 规则？（不使用 .toBeTruthy() 兜底、不使用 filter(Boolean) 绕过渲染异常、不使用 try/catch 吞断言）

## 失败处理
任一检查项不通过 → 打回 probe subagent 补充，最多重试 1 次。
