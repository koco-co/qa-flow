# 【0111版本】数据质量（XMind）
> 来源：zentao-cases/XMind/定制化/信永中和/信永中和测试用例.xmind

---

### 质量监控规则
#### 搜索
- 验证规则名称搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证规则ID搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证表名搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证规则类型搜索
- 枚举类搜索组件
- 支持多选
- 支持选择一致性、有效性、准确性、及时性、唯一性、完鍪性、特殊性、未定义类，支持多选
- 搜索结果正确
- 验证组合搜索
- 不同搜索条件组合搜索，取and关系
- 搜索结果正确
- 验证重置
- 点击重置清空所有搜索项
- 列表刷新展示全部数据
#### 列表
- 验证分区表达式目录树
- 列表左侧显示分区表大师目录
- 目录树展示正确
- 仅支持单选，选择后列表展示和该分区表达式一致的匹配的质量规则数据
- 搜索结果正确
- 选择分区表达式目录搜索结果与其他文本型、枚举型搜索结果取and关系
- 搜索结果正确
- 验证折叠未定义类的规则
- 默认勾选
- 显示正确
- 支持取消勾选
- 列表展示全量数据
- 勾选时过滤规则类型=‘未定义类’的结果
- 搜索结果正确
- 验证数据最近同步时间
- 同步任务完成时，查看「数据同步时间」
- 显示最后一次成功的同步完成时间，时间正确
- 验证立即同步
- 点击同步数据，立刻发起同步任务
- 同步成功，质量监控规则列表数据更新
- 同步任务未结束时，再次点击同步校验
- 提示：任务进行中，请稍后再试
- 同步任务完成时，查看「数据同步时间」
- 显示最后一次成功的同步完成时间，时间正确
- dataworks质量监控规则字段值变更，点击同步
- 同步成功，数据门户数据同步更新规则
- dataworks质量监控规则新增后，点击同步
- 同步成功，数据门户数据同步新增规则
- dataworks质量监控规则删除后，点击同步
- 同步成功，数据门户数据详情里面字段’dw是否已删除‘-已删除，不支持恢复
- 验证定时同步
- 每小时会定时发起同步任务
- 每一小时向dataworks请求数据更新，且同步成功
- 如10：00定时自动同步成功
- 「数据同步时间」时间正确，列表数据更新
- 验证列表展示
- 列表数据来源
- 数据来自DataWorks-数据质量产品-质量监控规则
- 列表字段包含规则名称、规则类型、规则ID、表名、规则字段、强/弱、操作按钮
- 显示正确
- 默认显示10条，大于10条分页展示
- 分页正确
- 验证列表排序
- 默认以‘规则ID’倒序排序
- 排序正确
- 规则名称、规则类型、规则ID、表名、规则字段、强/弱 均支持排序
- 默认同时只支持一种类型排序
- 点击字段旁的‘↕’图标切换正序、倒序
- 排序正确
#### 操作
- 详情
- 验证详情按钮
- 点击列表的【详情】按钮
- 可展开该模块抽屉式详情
- 点击灰层或返回
- 成功收起详情
- 验证详情tab
- 支持详情规则、管理问题、管理知识tab切换
- 切换成功，内容展示正确
- 验证详情-规则详情
- 引擎/数据源、引擎/数据源实例、规则名称、规则ID、规则类型、DW是否已删除、表名称、规则字段、强/弱、采样方式、校验规则描述、校验方式、比较方式、橙色阈值、红色阈值、期望值、过滤条件
- 字段及字段值正确
- 若dataworks以上字段发生变更，并重新同步成功
- 变更的字段值同步更新
- 验证详情-关联知识：该tab是否展示
- 当不存在任何关联知识时
- 不显示本TAB
- 当存在关联知识大于等于1条
- 显示本TAB
- 验证详情-关联知识列表
- 列表显示知识名称、更新人、最后更新时间、操作
- 字段及字段值正确
- 默认一页展示10条，大于10条分页展示
- 分页正确
- 验证详情-关联知识：点击详情
- 点击详情
- 在本页面上再打开知识条目详情弹窗（不校验用户是否有【质量知识库】的权限）
- 验证实例
- 点击【实例】自动带入本规则名称作为筛选条件
- 成功进入【数据质量-质量任务示例】列表，且筛选结果正确
- 验证关注
- 点击【关注】
- 关注成功
- 点击【取关】
- 取关成功

### 质量任务实例
#### 搜索
- 验证分区表达式搜索
- 枚举类搜索组件
- 支持多选
- 支持选择，选择内容来源于字典配置，支持多选
- 搜索结果正确
- 验证表名称搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证节点ID搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证业务日期搜索
- 时间类搜索组件
- 时间选择器yyyy-MM-dd HH:mm:ss
- 选择开始时间、结束时间，最小值必须小于等于最大值，左右为闭区间
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证执行开始时间搜索
- 时间类搜索组件
- 时间选择器yyyy-MM-dd HH:mm:ss
- 选择开始时间、结束时间，最小值必须小于等于最大值，左右为闭区间
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证执行结束时间搜索
- 时间类搜索组件
- 时间选择器yyyy-MM-dd HH:mm:ss
- 选择开始时间、结束时间，最小值必须小于等于最大值，左右为闭区间
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证状态搜索
- 枚举类搜索组件
- 支持多选
- 支持选择正常、报警，支持多选
- 搜索结果正确
- 验证组合搜索
- 不同搜索条件组合搜索，取and关系
- 搜索结果正确
- 验证重置
- 点击重置清空所有搜索项
- 列表刷新展示全部数据
#### 列表
- 验证只看我已在门户关注的规则
- 默认勾选
- 显示正确
- 支持取消勾选
- 列表展示全量数据
- 勾选时过滤规则类型=‘未关注’的结果
- 过滤掉所有不含用户关注规则的实例
- 验证数据最近同步时间
- 同步任务完成时，查看「数据同步时间」
- 显示最后一次成功的同步完成时间，时间正确
- 验证立即同步
- 点击同步数据，立刻发起同步任务
- 同步成功，质量任务实例列表数据更新
- 同步任务未结束时，再次点击同步校验
- 提示：任务进行中，请稍后再试
- 同步任务完成时，查看「数据同步时间」
- 显示最后一次成功的同步完成时间，时间正确
- dataworks质量任务实例新增后，点击同步
- 同步成功，数据门户数据同步新增实例
- dataworks质量规则变更点击同步
- 同步成功，历史实例规则数据不会变更
- 验证定时同步
- 每小时会定时发起同步任务
- 每一小时向dataworks请求数据更新，且同步成功
- 如10：00定时自动同步成功
- 「数据同步时间」时间正确，列表数据更新
- 验证列表展示
- 列表数据来源
- 数据来自DataWorks-数据质量产品-质量规则任务实例
- 列表字段包含表名、节点ID、分区表达式、业务日期、执行开始时间、执行结束时间、状态、规则数、操作按钮
- 显示正确
- 默认显示10条，大于10条分页展示
- 分页正确
- 验证列表排序
- 默认以‘业务日期’倒序排序
- 排序正确
- 表名、节点ID、分区表达式、业务日期、执行开始时间、执行结束时间、状态、规则数 均支持排序
- 默认同时只支持一种类型排序
- 点击字段旁的‘↕’图标切换正序、倒序
- 排序正确
#### 操作
- 详情
- 验证详情按钮
- 点击列表的【详情】按钮
- 成功展开该模块抽屉式详情
- 点击灰层或返回
- 成功收起详情
- 验证详情tab
- 规则tab根据实例包含规则显示，根据规则ID顺序排列横向排列，最后显示关联问题TAB
- 支持tab切换，切换成功，内容展示正确
- 规则tab名称显示规则名称
- 显示正确
- 验证规则tab对应质量知识库搜索及排序
- 选中当前规则tab
- 显示关联规则ID=本规则的知识条目卡片式列表，且仅展示已发布的知识条目
- 支持对关联规则名称/关联表名称/知识条目名称/知识条目描述的关键字进行模糊搜索
- 搜索结果正确
- 默认以查看次数降序排序
- 排序正确
- 验证规则tab对应质量知识库列表
- 字段显示条目名称、描述、查看次数
- 字段正确
- 查看次数超过9999时
- 显示9999+
- 查看次数小于等于9999时
- 按照实际值显示
- 验证规则tab对应质量知识库列表，查看详情
- 点击【详情】
- 成功在本页面上再打开知识条目详情弹窗（不校验用户是否有【质量知识库】的权限）
- 详情所有字段均正确展示
- 字段及字段值正确
- 若质量知识库编辑修改保存成功
- 此处修改字段值同步更新
- 点击【返回列表】
- 成功返回列表页
- 验证规则tab对应质量知识库列表，查看详情-搜索
- 支持选择关联规则、关联表名称、知识条目描述、知识条目名称，仅支持选择
- 选择成功
- 选择成功后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证规则tab对应质量知识库列表，查看详情-知识条目基本信息
- 包含知识条目名称、关联表名、关联系统、知识条目状态、知识条目发布人、知识条目发布时间、最后更新人、最后更新时间、知识条目类型、关联质量规则、知识条目名称描述
- 字段及字段值均正确
- 验证规则tab对应质量知识库列表，查看详情-文档附件
- 列表显示序号、文件名称、操作
- 字段及字段值均正确
- 一页展示5条，大于5条分页展示
- 分页正确
- 点击下载
- 下载成功，查看下载文件内容正确

### 质量知识库
#### 搜索
- 验证知识条目名称搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证关联表名搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证关联系统搜索
- 枚举类搜索组件
- 支持多选
- 支持选择，选择内容来源于字典配置，支持多选
- 搜索结果正确
- 验证知识条目类型搜索
- 枚举类搜索组件
- 支持多选
- 支持选择，选择内容来源于字典配置，支持多选
- 搜索结果正确
- 验证最后更新时间搜索
- 时间类搜索组件
- 时间选择器yyyy-MM-dd HH:mm
- 选择开始时间、结束时间，最小值必须小于等于最大值，左右为闭区间
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证知识条目发布人姓名搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证知识条目状态搜索
- 枚举类搜索组件
- 支持多选
- 支持选择已发布、未发布，支持多选
- 搜索结果正确
- 验证关联质量规则名称搜索
- 文本类搜索组件
- 需要选择匹配模式：等于、包含、不包含、首匹配、尾匹配，默认选择包含，且仅支持单选
- 选择不同的匹配模式后，在文本框输入内容，点击查询
- 搜索结果正确，有数据展示相应数据，无数据展示暂无数据
- 验证组合搜索
- 不同搜索条件组合搜索，取and关系
- 搜索结果正确
- 验证重置
- 点击重置清空所有搜索项
- 列表刷新展示全部数据
#### 列表
- 验证列表展示
- 显示知识条目名称、关联表名、关联系统、关联规则、知识条目发布人、知识条目发布时间、最后更新时间、知识条目类型、知识条目状态、操作
- 字段及字段值均正确
- 默认显示10条，大于10条分页展示
- 分页正确
- 验证列表排序
- 默认以‘最后更新时间’倒序排序
- 排序正确
- 知识条目名称、关联表名、关联系统、关联规则、知识条目发布人、知识条目发布时间、最后更新时间、知识条目类型、知识条目状态 均支持排序
- 默认同时只支持一种类型排序
- 点击字段旁的‘↕’图标切换正序、倒序
- 排序正确
#### 操作
- 新增
- 验证点击新增
- 点击新增
- 成功以抽屉式弹窗唤出新建条目页面
- 知识条目基本信息：知识条目名称、关联表名、关联系统、知识条目状态、知识条目类型、关联质量规则、描述
- 正确填写
- 文档附件
- 上传成功
- 所以字段均正确填写，点击保存
- 保存成功，列表+1
- 点击灰层或返回按钮，二次确认‘是否确认关闭，未提交内容将不会被保存’，点击确认
- 关闭成功，返回列表页，列表数量不增加
- 验证新增-知识条目名称
- 必填
- 为空提示：“知识条目名称不能为空”
- 长度1-30位
- 超长提示：“知识条目名称长度必须为1-30位”
- 不支持重复
- 重复提示：‘名称已存在’
- 验证新增-知识条目描述
- 必填
- 为空提示：“知识条目描述不能为空”
- 长度1-2000字
- 超长提示：“知识条目描述长度必须为1-2000字”
- 验证新增-知识条目类型
- 必填
- 为空提示：“知识条目类型不能为空”
- 字典配置
- 字段正确
- 验证新增-关联规则
- 非必填
- 支持为空
- 集合元素0-100个
- “关联规则数量超出限制，最多 100个”
- 验证新增-关联系统
- 非必填
- 支持为空
- 集合元素0-100个
- “关联系统数量超出限制，最多 100个”
- 字典来源
- 数据入湖-源系统
- 验证新增-附件上传
- 同一知识条目上传大于10个
- 无法上传，提示
- 同一知识条目上传小于等于10个附件
- 上传成功，附件列表同步更新
- 上传文件格式支持.pdf/ .xls/ .xlsx/ .doc/ .docx/ .ppt/ .pptx，且单文件≤100 MB
- 上传成功
- 上传格式非.pdf/ .xls/ .xlsx/ .doc/ .docx/ .ppt/ .pptx格式
- 上传失败，提示格式不正确
- 上传单文件大小>100MB
- 上传失败，提示：单文件大小>100MB
- 验证新增-附件移除
- 点击【移除】
- 成功删除已上传的文件，列表-1
- 删除成功后，如不足10个还支持重新上传
- 上传成功
- 验证点击编辑按钮
- 点击编辑
- 成功以抽屉式弹窗唤出新建条目页面
- 知识条目基本信息：知识条目名称、关联表名、关联系统、知识条目状态、知识条目类型、关联质量规则、描述
- 支持修改，校验规则同新增
- 文档附件
- 支持移除，重新上传，校验规则同新增
- 编辑时所以字段均正确填写，点击保存
- 保存成功，查看列表及详情、已经被引用的地方，修改字段值同步更新
- 再次点击编辑
- 成功回显上传编辑成功的内容
- 点击灰层或返回按钮，二次确认‘是否确认关闭，未提交内容将不会被保存’，点击确认
- 关闭成功，返回列表页，编辑内容不会保存
- 验证点击删除
- 点击删除，二次确认‘是否确认删除？本操作不可撤回‘ 点击确定
- 删除成功，列表-1
- 删除成功后
- 规则相关的附件删除
