# 【1223版本】数据货架-指标资源（#9830）（XMind）
> 来源：zentao-cases/XMind/定制化/信永中和/信永中和测试用例.xmind

---

### 指标资源
#### 验证指标资源列表数据来源
- 从指标体系平台同步全量指标，门户系统过滤仅展示已上架指标
- 数据正确
#### 验证输入搜索
- 输入资源名称，点击搜索，支持模糊搜索
- 搜索结果正确
#### 验证左侧数据目录树 选择
- 数据目录树来源：同步指标体系平台的业务领域-主题域分组-主题域目录
- 数据正确
- 默认展开1级节点
- 显示正确
- 点击下级节点展开
- 展开成功
- 点击下级节点收起
- 收起成功
- 仅支持单选，选中非末级节点
- 筛选结果正确，结果包含本级及其所有下级的所有指标
#### 验证组合搜索
- 指标搜索与指标目录的筛选取and逻辑
- 组合搜索结果正确
#### 验证筛选结果数量
- 筛选后显示，筛选结果：共xx个结果
- 筛选数量正确
#### 【1223版本不验证】验证前往指标平台
- 点击前往指标平台
- 成功以新窗口打开，单点登录指标平台-指标清单页面
#### 【1223版本不验证】验证数据同步时间展示
- 展示指标资源最后一次成功同步的完成时间
- 数据同步时间：年月日时分秒，时间正确
#### 验证列表排序
- 默认按照新增日期倒序展示
- 排序正确
- 支持切换正序倒序，点击正序
- 排序按照新增日期正序排列
- 点击倒序
- 排序按照新增日期倒序排列
#### 验证指标列表
- 列表显示：指标层级、指标名称、指标编码、业务领域/主题域分组/主题域、管理部门、计量单位、新增时间
- 字段及字段值正确
#### 验证点击指标详情
- 点击指标详情
- 成功进入指标详情页面
- 顶部展示：指标名称（指标编码）
- 回显正确
- 详情所有字段及字段值：来源于指标体系平台同步的指标详细信息
- 回显正确
- 点击返回上层
- 页签选中指标列表页或者重新打开指标列表页
#### 验证指标详情-【基本信息】
- 基本信息
- 字段及字段值正确
- 点击前往指标中台查看完整信息【1223版本不验证】
- 成功跳转到指标体系平台中对应指标的详情页
- 基本属性字段包含：指标编号
、指标名称、英文简称、指标别名
、新增日期、业务领域、主题域分组
、主题域、指标层级、体系一级、体系二级
、是否重点指标、统计周期、刷新频率、计量单位
、统计维度、统计对象、指标管理公司、指标管理部门、指标责任人、是否基础数据、指标业务来源
、指标数据来源、安全等级
、指标生效周期、指标定义、设置目的
、计算公式、绿灯判定规则、黄灯判定规则、红灯判定规则、备注
- 中核指标平台-元数据同步，基本信息同名字段，字段及字段值正确
- 技术属性字段包含：填报部门
、填报人员、计量单位、数据表、数据来源数据库
、数据模型、数据字段、数据格式、指标报表分享地址
- 中核指标平台-元数据同步，基本信息同名字段，字段及字段值正确
#### 验证指标详情-【技术-维度信息】
- 一页展示5行数据，超过分页展示
- 分页正确
- 列表显示序号、维度编号、维度名称
- 中核指标平台-元数据同步，维度信息同名字段，列表数据正确
#### 验证指标详情-【上级指标信息】
- 一页展示5行数据，超过分页展示
- 分页正确
- 列表显示序号、上级指标信息编号、上级指标名称、开始日期、结束日期
- 中核指标平台-元数据同步，上级指标信息同名字段，列表数据正确
#### 验证指标详情-【下级指标信息】
- 一页展示5行数据，超过分页展示
- 分页正确
- 列表显示序号、下级指标信息编号、下级指标名称、开始日期、结束日期
- 中核指标平台-元数据同步，下级指标信息同名字段，列表数据正确
