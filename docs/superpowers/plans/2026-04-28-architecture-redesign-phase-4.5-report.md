# P4.5 dtstack-cli → dtstack-sdk Rename Report

> **Status**: Complete · **Date**: 2026-04-28
> **Scope**: spec §3.2 npm package rename; shell command name preserved

## Changes Landed

| Surface | Change |
|---------|--------|
| Directory | tools/dtstack-cli/ → tools/dtstack-sdk/ (git mv) |
| package.json name | "dtstack-cli" → "dtstack-sdk" |
| package.json bin | UNCHANGED ({"dtstack-cli": "src/cli.ts"}) |
| Root workspaces | ["tools/dtstack-cli", "engine"] → ["tools/dtstack-sdk", "engine"] |
| bun.lock | regenerated by bun install |
| workspace/dataAssets/shared/helpers/preconditions.ts | 3 imports rewritten |
| CLAUDE.md line 13 | usage.md path updated; command name in table preserved |
| test-case-gen rules.md line 309 | "dtstack-cli 包" → "dtstack-sdk 包" |

## Side-Cleanups (P4 Fixup fallout)

| Item | Fix |
|------|-----|
| standardize-agent A4 | owner_skill case-format → test-case-gen (4 cross-skill refs) |
| assertion-fidelity A4 | promoted to docs/architecture/references/, 3 agent paths updated |
| playwright-cli S4 | SKILL.md 359→33 lines, body to workflow.md (348 lines) |
| merge-specs tsc gate | removed types:["node"] from compileCheck tsconfig |
| dtstack-sdk CLI tests | 3 test files had old paths (tools/dtstack-cli/) — fixed to relative |

## Verification

- bun test --cwd engine: 157 pass (held)
- bun test --cwd tools/dtstack-sdk: 50 pass 0 fail (was 35/8 before fix)
- bunx dtstack-cli --help: identical pre/post output (user perspective unchanged)
- Smoke imports resolve: DtStackClientLike, precondSetup, createClientFromPage

## Final Reference Sweep

- ts/js imports from "dtstack-cli": **0 hits**
- package.json/bin "dtstack-cli" (non-bin): **0 hits**
- tools/dtstack-cli path in docs (non-historical): **0 hits**

## Known Followups

- If a future lanhu-sdk consolidation happens, follow same tools/{name}-sdk/ form.

## Decisions for Next Phase (P5)

- P4.5 leaves engine + tools/ in their final v3 shape.
- P5 (path treatment / 131+ kata-cli refs in scripts and docs) can proceed.
