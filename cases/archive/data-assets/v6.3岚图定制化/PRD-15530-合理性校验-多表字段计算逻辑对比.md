---
name: 【6.3岚图定制化分支】合理性校验-多表字段值对比（计算逻辑）
description: 合理性校验-多表字段值对比（计算逻辑）
tags:
  - 数据质量
  - 质量规则
  - 合理性校验
  - 多表字段对比
  - 规则配置
  - 任务实例
  - 质量报告
  - 岚图
  - 多表字段值对比
  - 数据资产
module: data-assets
source: cases/requirements/data-assets/Story-20260329/temp/final-reviewed.json
case_count: 47
created_at: 2026-03-29
origin: json
---

# 【6.3岚图定制化分支】合理性校验-多表字段值对比（计算逻辑）

---

## 数据质量

### 规则配置-多表字段值对比（计算逻辑）

#### P0 冒烟-主流程

##### 验证合理性校验-多表字段值对比（计算结果对比模式）完整配置后保存成功 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示，页面顶部有【新建规则】按钮 |
| 2 | 点击【新建规则】按钮，在弹出的表单中选择数据源为「Doris-test_db」，数据表为「test_table_a」 | 表单进入规则编辑步骤，展示规则类型选择项 |
| 3 | 在「规则类型」下拉框选择「合理性校验」 | 表单下方出现「合理性类型」子选项 |
| 4 | 在「合理性类型」下拉框选择「多表字段值对比」 | 表单展示多表字段值对比配置项，包含：字段、计算逻辑配置、对比方法、比较方式（默认选中「计算结果对比」）、关联表配置区域 |
| 5 | 在「* 字段」下拉框选择「score」字段 | 「字段」下拉框仅展示数值型字段（score DOUBLE、amount DECIMAL），选中 score 后显示在输入框中 |
| 6 | 在「比较方式」中选中「计算结果对比」单选项 | 「计算逻辑配置2」输入框出现在表单中 |
| 7 | 在「计算逻辑配置1」编辑框旁的字段选择器中点击「score」字段 | 编辑框自动填充内容为「表a.score」 |
| 8 | 在「计算逻辑配置1」编辑框中手动追加输入「 + 」 | 编辑框内容变为「表a.score + 」，光标位于末尾 |
| 9 | 在「* 对比方法」下拉框选择「>」 | 对比方法显示「>」 |
| 10 | 在「计算逻辑配置2」编辑框旁的字段选择器中点击「value」字段（关联表 test_table_b 下） | 编辑框自动填充内容为「表b.value」 |
| 11 | 在关联表配置区域点击【添加关联表】按钮，在新增一行中： - 关联表：选择「test_table_b」 - 关联表主键：选择「id」 - 校验表主键：选择「id」 | 新增一行关联表配置，三个必填字段均可正常选择，悬浮「关联表主键」字段时出现提示「根据主键关联一致的数据进行计算校验」 |
| 12 | 点击【保存】按钮 | 页面提示「保存成功」，规则配置列表中新增一条记录，规则类型显示「合理性校验」，合理性类型显示「多表字段值对比」 |

##### 验证合理性校验-多表字段值对比（计算结果值判断模式）完整配置后保存成功 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】按钮，选择数据源「Doris-test_db」，数据表「test_table_a」，规则类型「合理性校验」，合理性类型「多表字段值对比」 | 多表字段值对比配置表单正常展示，「比较方式」默认选中「计算结果对比」 |
| 3 | 在「比较方式」中切换选中「计算结果值判断」单选项 | 「计算逻辑配置2」输入框消失；表单中出现「结果值」比较符下拉框和固定比较值输入框 |
| 4 | 在「* 字段」下拉框选择「amount」字段 | 字段选择成功，显示「amount」 |
| 5 | 在「计算逻辑配置1」编辑框中点击字段选择器的「amount」字段，再手动追加「 * 2」 | 编辑框内容为「表a.amount * 2」 |
| 6 | 在关联表配置区域点击【添加关联表】按钮，在新增一行中： - 关联表：选择「test_table_b」 - 关联表主键：选择「id」 - 校验表主键：选择「id」 | 关联表配置行正常填写，三个必填字段均已选择 |
| 7 | 在「结果值」比较符下拉框选择「>=」 | 结果值比较符显示「>=」 |
| 8 | 在固定比较值输入框输入「100」 | 输入框显示「100」 |
| 9 | 点击【保存】按钮 | 页面提示「保存成功」，规则配置列表中新增一条记录，合理性类型显示「多表字段值对比」 |

#### 模式切换交互

##### 验证切换「计算结果对比」模式时「计算逻辑配置2」字段出现 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】按钮，选择规则类型「合理性校验」，合理性类型「多表字段值对比」 | 多表字段值对比配置表单展示，「比较方式」默认选中「计算结果对比」，「计算逻辑配置2」输入框可见 |
| 3 | 在「比较方式」中点击「计算结果值判断」单选项 | 「计算逻辑配置2」输入框从表单中隐藏；表单中出现「结果值」比较符下拉框和固定比较值输入框 |
| 4 | 在「比较方式」中点击「计算结果对比」单选项 | 「结果值」比较符下拉框和固定比较值输入框隐藏；「计算逻辑配置2」输入框重新出现在表单中 |

##### 验证切换「计算结果值判断」模式时「计算逻辑配置2」字段隐藏且已填内容不再可见 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，确认当前「比较方式」默认为「计算结果对比」，「计算逻辑配置2」输入框可见 | 表单中「计算逻辑配置2」输入框可见 |
| 3 | 在「计算逻辑配置2」输入框中通过字段选择器填入「表a.score」，再追加「 + 10」 | 「计算逻辑配置2」内容为「表a.score + 10」 |
| 4 | 在「比较方式」中点击「计算结果值判断」单选项 | 「计算逻辑配置2」输入框从表单中隐藏，之前填入的内容不再可见；表单出现「结果值」比较符下拉框和固定比较值输入框 |

#### 字段选择器-数值型约束

##### 验证「字段」下拉框仅展示数值型字段 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择数据源「Doris-test_db」，数据表「test_table_mix」，规则类型「合理性校验」，合理性类型「多表字段值对比」 | 多表字段值对比配置表单展示 |
| 3 | 点击「* 字段」下拉框，展开字段列表 | 字段列表中展示且仅展示数值型字段：id（INT）、score（DOUBLE）、amount（DECIMAL）；非数值型字段 name（VARCHAR）、flag（BOOLEAN）、remark（TEXT）不在列表中出现 |

##### 验证计算逻辑配置的字段选择器中仅展示数值型字段 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择数据源「Doris-test_db」，数据表「test_table_mix」，规则类型「合理性校验」，合理性类型「多表字段值对比」，并添加关联表 test_table_b | 表单展示完整，关联表 test_table_b 已添加 |
| 3 | 点击「计算逻辑配置1」右侧的字段选择器，展开字段树 | 字段选择器中仅展示各表的数值型字段：校验表 test_table_mix 下显示 id（INT）、score（DOUBLE），不显示 name（VARCHAR）；关联表 test_table_b 下显示 id（INT）、value（FLOAT），不显示 label（VARCHAR） |

#### 计算逻辑编辑框交互

##### 验证点击字段选择器字段名自动填充到计算逻辑编辑框并带表别名 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择数据源「Doris-test_db」，数据表「test_table_a」、规则类型「合理性校验」、合理性类型「多表字段值对比」，依次添加关联表 test_table_b 和 test_table_c | 表单中关联表列表显示两行：test_table_b（别名 b）和 test_table_c（别名 c） |
| 3 | 在「计算逻辑配置1」编辑框为空的情况下，点击字段选择器中校验表「test_table_a」下的「score」字段 | 「计算逻辑配置1」编辑框填充内容「表a.score」（系统将校验表自动分配别名 a） |
| 4 | 在「计算逻辑配置1」编辑框中手动追加输入「 + 」，再点击字段选择器中关联表「test_table_b」下的「value」字段 | 「计算逻辑配置1」编辑框内容为「表a.score + 表b.value」 |
| 5 | 继续追加输入「 + 」，再点击字段选择器中关联表「test_table_c」下的「qty」字段 | 「计算逻辑配置1」编辑框内容为「表a.score + 表b.value + 表c.qty」 |

##### 验证计算逻辑编辑框支持手动输入运算符 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择数据源「Doris-test_db」，数据表「test_table_a」，进入多表字段值对比配置表单 | 表单展示，「计算逻辑配置1」编辑框可用 |
| 3 | 通过字段选择器点击「score」字段，编辑框填入「表a.score」后，手动在编辑框中输入「 - 」 | 编辑框内容变为「表a.score - 」，运算符被正确追加到现有内容之后 |
| 4 | 再通过字段选择器点击「amount」字段 | 编辑框内容变为「表a.score - 表a.amount」，字段自动填充追加在运算符之后 |
| 5 | 继续手动在编辑框末尾输入「 * 0.5」 | 编辑框内容变为「表a.score - 表a.amount * 0.5」，手动输入内容正确追加 |

##### 验证计算逻辑编辑框内容超出固定展示长度时鼠标悬浮可查看全部内容 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择数据源「Doris-test_db」，数据表「test_table_a」，规则类型「合理性校验」，合理性类型「多表字段值对比」，添加关联表 test_table_b | 表单展示，「计算逻辑配置1」编辑框可用 |
| 3 | 在「计算逻辑配置1」编辑框中通过字段选择器依次填入「表a.score + 表a.amount + 表a.qty + 表b.value + 表b.total」，使内容超出编辑框固定展示宽度 | 编辑框内容超出固定显示宽度后被截断显示；将鼠标悬浮在编辑框上，出现 Tooltip 展示完整内容「表a.score + 表a.amount + 表a.qty + 表b.value + 表b.total」 |

#### 关联表配置

##### 验证点击「添加关联表」按钮新增一行关联表配置 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」 | 表单展示，关联表配置区域显示【添加关联表】按钮，关联表列表当前为空 |
| 3 | 点击【添加关联表】按钮 | 关联表列表新增一行，包含以下可配置字段：关联表（数据表选择器）、关联表主键（下拉选择）、校验表主键（下拉选择），悬浮「关联表主键」字段时出现提示「根据主键关联一致的数据进行计算校验」 |
| 4 | 再次点击【添加关联表】按钮 | 关联表列表新增第二行，共 2 行配置，每行独立可配置 |

##### 验证删除关联表行后行数减少且列表正确刷新 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，连续点击【添加关联表】3 次 | 关联表列表共显示 3 行配置 |
| 3 | 点击第 2 行右侧的删除按钮 | 第 2 行从列表中移除，列表剩余 2 行，原第 3 行上移为第 2 行 |

##### 验证添加第 10 个关联表时操作成功，添加第 11 个时提示已达上限 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，连续点击【添加关联表】9 次 | 关联表列表显示 9 行配置 |
| 3 | 第 10 次点击【添加关联表】按钮 | 关联表列表新增第 10 行，成功添加，列表共显示 10 行 |
| 4 | 第 11 次点击【添加关联表】按钮 | 页面出现提示信息，提示已达到最大关联表数量上限（10 个），关联表列表仍为 10 行，不再新增行 |

#### 必填字段校验

##### 验证「字段」为空时无法保存并展示校验提示 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果对比」，填写计算逻辑配置1「表a.score」、对比方法「>」、计算逻辑配置2「表b.value」，添加关联表配置（关联表 test_table_b、关联表主键 id、校验表主键 id），但不选择「* 字段」 | 表单其他字段均已填写，「* 字段」下拉框为空 |
| 3 | 点击【保存】按钮 | 「* 字段」下拉框下方展示红色校验提示「请选择字段」，页面不跳转，保存失败 |

##### 验证「计算逻辑配置1」为空时无法保存并展示校验提示 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果对比」，选择字段「score」、对比方法「>」、填写计算逻辑配置2「表b.value」，添加关联表配置（关联表 test_table_b、关联表主键 id、校验表主键 id），但「计算逻辑配置1」编辑框保持为空 | 表单其他字段均已填写，「* 计算逻辑配置1」编辑框为空 |
| 3 | 点击【保存】按钮 | 「* 计算逻辑配置1」编辑框附近展示红色校验提示，页面不跳转，保存失败 |

##### 验证「对比方法」为空时无法保存并展示校验提示 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果对比」，选择字段「score」、填写计算逻辑配置1「表a.score」、计算逻辑配置2「表b.value」，添加关联表配置（关联表 test_table_b、关联表主键 id、校验表主键 id），但「* 对比方法」下拉框不选择 | 表单其他字段均已填写，「* 对比方法」下拉框为空 |
| 3 | 点击【保存】按钮 | 「* 对比方法」下拉框下方展示红色校验提示「请选择对比方法」，页面不跳转，保存失败 |

##### 验证「计算结果对比」模式下「计算逻辑配置2」为空时无法保存 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果对比」，选择字段「score」，填写计算逻辑配置1「表a.score」，对比方法选「>」，添加关联表配置（关联表 test_table_b、关联表主键 id、校验表主键 id），「计算逻辑配置2」编辑框保持为空 | 表单中「计算逻辑配置2」为空，其他字段均已填写 |
| 3 | 点击【保存】按钮 | 「* 计算逻辑配置2」编辑框附近展示红色校验提示，页面不跳转，保存失败 |

##### 验证「计算结果值判断」模式下「结果值比较符」为空时无法保存 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果值判断」，选择字段「score」，填写计算逻辑配置1「表a.score」，添加关联表配置（关联表 test_table_b、关联表主键 id、校验表主键 id），固定比较值输入「100」，「结果值」比较符下拉框不选择 | 表单中「结果值」比较符为空，其他字段均已填写 |
| 3 | 点击【保存】按钮 | 「结果值」比较符下拉框附近展示红色校验提示，页面不跳转，保存失败 |

##### 验证「计算结果值判断」模式下固定比较值为空时无法保存 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果值判断」，选择字段「score」，填写计算逻辑配置1「表a.score」，添加关联表配置（关联表 test_table_b、关联表主键 id、校验表主键 id），结果值比较符选「>」，固定比较值输入框保持为空 | 表单中固定比较值输入框为空，其他字段均已填写 |
| 3 | 点击【保存】按钮 | 固定比较值输入框附近展示红色校验提示「请输入比较值」，页面不跳转，保存失败 |

##### 验证关联表行的「关联表」字段为空时无法保存 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，「比较方式」选「计算结果对比」，选择字段「score」，填写计算逻辑配置1「表a.score」，对比方法「>」，计算逻辑配置2「表b.value」，点击【添加关联表】后第一行「关联表」字段不选择 | 关联表第一行「关联表」字段为空 |
| 3 | 点击【保存】按钮 | 关联表第一行「* 关联表」字段附近展示红色校验提示，页面不跳转，保存失败 |

##### 验证关联表行的「关联表主键」字段为空时无法保存 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，完成除关联表主键外的所有必填字段配置，在关联表配置行中关联表选「test_table_b」，校验表主键选「id」，「关联表主键」不选择 | 关联表配置行「* 关联表主键」字段为空 |
| 3 | 点击【保存】按钮 | 「* 关联表主键」下拉框附近展示红色校验提示，页面不跳转，保存失败 |

##### 验证关联表行的「校验表主键」字段为空时无法保存 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，选择规则类型「合理性校验」，合理性类型「多表字段值对比」，完成除校验表主键外的所有必填字段配置，在关联表配置行中关联表选「test_table_b」，关联表主键选「id」，「校验表主键」不选择 | 关联表配置行「* 校验表主键」字段为空 |
| 3 | 点击【保存】按钮 | 「* 校验表主键」下拉框附近展示红色校验提示，页面不跳转，保存失败 |

#### 过滤条件配置

##### 验证配置过滤条件后规则保存成功且过滤条件生效 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，完成多表字段值对比（计算结果对比模式）基础配置：字段「score」、计算逻辑配置1「表a.score」、对比方法「>」、计算逻辑配置2「表b.value」，添加关联表 test_table_b（关联表主键 id，校验表主键 id） | 基础配置完成，可见过滤条件配置区域 |
| 3 | 在过滤条件配置区域，「字段」下拉框选择「status」，比较方式选「固定值」，比较符选「>」，固定值输入框输入「0」 | 过滤条件配置行展示完整：字段「status」，比较方式「固定值」，比较符「>」，固定值「0」 |
| 4 | 点击【保存】按钮 | 页面提示「保存成功」，规则配置列表中新增包含过滤条件的规则记录 |

##### 验证不配置过滤条件时规则可正常保存 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，完成多表字段值对比（计算结果对比模式）基础配置：字段「score」、计算逻辑配置1「表a.score」、对比方法「>」、计算逻辑配置2「表b.value」，添加关联表 test_table_b（关联表主键 id，校验表主键 id），过滤条件区域所有字段保持为空 | 过滤条件区域所有字段均为空，无填写 |
| 3 | 点击【保存】按钮 | 页面提示「保存成功」，规则配置列表中新增规则记录，无过滤条件相关报错 |

#### 规则入口路由

##### 验证选择「合理性校验」规则类型后出现「合理性类型」子选项 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击【新建规则】，在弹出的表单中选择数据源「Doris-test_db」和数据表「test_table_a」，点击下一步进入规则类型选择 | 展示规则类型选择区域，当前未选择任何类型 |
| 3 | 在「规则类型」下拉框选择「合理性校验」 | 表单下方出现「合理性类型」子选项区域 |
| 4 | 在「合理性类型」下拉框中查看可选项 | 下拉列表中包含「多表字段值对比」选项 |
| 5 | 选择「多表字段值对比」 | 表单展开多表字段值对比配置区域，包含：字段、计算逻辑配置、对比方法、比较方式（默认「计算结果对比」）、关联表配置区域、过滤条件配置区域 |

### 规则库-多表字段值对比条目

#### 规则库展示

##### 验证规则库中新增「多表字段值对比」条目并展示正确内容 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 点击页面中的【规则库】入口，进入规则库列表页 | 规则库列表页正常展示，展示已内置的规则条目 |
| 3 | 在规则库列表中查找「规则分类」为「合理性校验」的规则 | 列表中包含「多表字段值对比」规则条目 |
| 4 | 查看「多表字段值对比」规则条目的各字段内容 | 该条目展示： - 规则名称：多表字段值对比 - 规则解释：多表字段值的计算对比 - 规则分类：合理性校验 - 关联范围：多表 - 规则描述：比较多表关联后字段值的计算逻辑是否符合要求，支持将计算结果进行对比或计算结果值的值域判断 |

##### 验证规则库中「多表字段值对比」条目可用于新建规则 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-规则配置】页面 | 规则配置列表页正常展示 |
| 2 | 进入规则库页面，找到「多表字段值对比」条目，点击该条目的新建/使用按钮（待核实：全新需求按钮名称以实际UI为准） | 系统跳转到规则配置新建页面，规则类型预填「合理性校验」，合理性类型预填「多表字段值对比」 |

## 数据质量-任务实例

### 任务实例列表页

#### 规则执行结果入口展示

##### 验证校验不通过时【查看详情】入口可点击 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载，显示任务实例列表 |
| 2 | 在任务实例列表中找到已执行的任务实例，点击该实例进入规则执行结果列表 | 规则执行结果列表展示，包含「合理性校验-多表字段值对比」规则一条记录，「质检结果」列显示「校验不通过」 |
| 3 | 查看「合理性校验-多表字段值对比」规则行的「操作」列 | 「操作」列显示【查看详情】按钮，且按钮处于可点击状态（非灰色禁用） |
| 4 | 点击【查看详情】按钮 | 弹出「查看『合理性校验-多表字段值对比』明细」弹窗，弹窗正常打开 |

##### 验证校验通过时【查看详情】入口不可点击或不显示 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载，显示任务实例列表 |
| 2 | 在任务实例列表中找到已执行的任务实例，点击该实例进入规则执行结果列表 | 规则执行结果列表展示，包含「合理性校验-多表字段值对比」规则一条记录，「质检结果」列显示「校验通过」 |
| 3 | 查看「合理性校验-多表字段值对比」规则行的「操作」列 | 「操作」列中【查看详情】按钮不显示，或处于灰色禁用状态，无法点击；系统不提供明细查看入口 |

##### 验证校验失败时展示【查看日志】按钮 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载，显示任务实例列表 |
| 2 | 在任务实例列表中找到已执行的任务实例，点击该实例进入规则执行结果列表 | 规则执行结果列表展示，包含「合理性校验-多表字段值对比」规则一条记录，「质检结果」列显示「校验失败」 |
| 3 | 查看「合理性校验-多表字段值对比」规则行的「操作」列 | 「操作」列显示【查看日志】按钮，且按钮处于可点击状态；不显示【查看详情】按钮 |
| 4 | 点击【查看日志】按钮 | 打开日志查看界面或弹窗，展示该规则执行失败的日志信息 |

##### 验证三种执行结果（通过/不通过/失败）入口按钮互斥显示 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载，显示任务实例列表 |
| 2 | 点击包含三条规则的任务实例，进入规则执行结果列表 | 规则执行结果列表展示三条规则记录 |
| 3 | 查看规则A（校验通过）行的「操作」列 | 规则A「操作」列不显示【查看详情】也不显示【查看日志】，或【查看详情】为禁用状态 |
| 4 | 查看规则B（校验不通过）行的「操作」列 | 规则B「操作」列显示可点击的【查看详情】按钮，不显示【查看日志】 |
| 5 | 查看规则C（校验失败）行的「操作」列 | 规则C「操作」列显示可点击的【查看日志】按钮，不显示【查看详情】 |

### 任务实例-明细弹窗

#### 明细数据列展示

##### 验证明细弹窗标题格式正确显示 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，找到「校验不通过」状态的「合理性校验-多表字段值对比」规则行，点击【查看详情】 | 明细弹窗打开，弹窗标题显示为「查看『合理性校验-多表字段值对比』明细」，格式完整 |

##### 验证明细弹窗展示校验表主键字段列 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，点击「校验不通过」行的【查看详情】打开明细弹窗 | 明细弹窗打开 |
| 3 | 查看明细弹窗的列头 | 列头包含「test_table_a.id」（校验表主键字段列），该列排列在最左侧；列头名称格式为「表名.字段名」 |

##### 验证明细弹窗展示校验表校验字段列且不符合规则数据标红 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，点击「校验不通过」行的【查看详情】打开明细弹窗 | 明细弹窗打开 |
| 3 | 查看明细弹窗列头及数据行内容 | 列头包含「test_table_a.score」（校验字段列），id=101 的数据行中，「test_table_a.score」列的值 30 以红色高亮标红展示，其余满足规则的行不标红 |

##### 验证明细弹窗动态展示校验表计算字段列 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，点击「校验不通过」行的【查看详情】打开明细弹窗 | 明细弹窗打开 |
| 3 | 查看明细弹窗的列头 | 列头动态包含「test_table_a.amount」（校验表计算字段列），该列按计算逻辑中的字段顺序展示，与配置的计算字段一致 |

##### 验证明细弹窗动态展示关联表主键字段列和参与计算的关联表字段列 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，点击「校验不通过」行的【查看详情】打开明细弹窗 | 明细弹窗打开 |
| 3 | 查看明细弹窗的列头 | 列头包含「test_table_b.ref_id」（关联表主键列）和「test_table_b.value」（关联表参与计算字段列），列名格式为「表名.字段名」，与配置的关联表信息一致 |

##### 验证明细弹窗主键字段列支持多主键多选配置 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，点击「校验不通过」行的【查看详情】打开明细弹窗 | 明细弹窗打开 |
| 3 | 查看明细弹窗的列头 | 列头同时包含「test_table_a.id」和「test_table_a.date」两个主键字段列，均展示在最左侧，两列均正确展示对应数据行的主键值 |

##### 验证明细弹窗表名支持复制功能 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-任务实例】页面 | 页面正常加载 |
| 2 | 点击任务实例进入规则执行结果列表，点击「校验不通过」行的【查看详情】打开明细弹窗 | 明细弹窗打开，列头格式为「表名.字段名」 |
| 3 | 在明细弹窗列头区域找到「test_table_a.id」列，触发复制表名操作（点击列头中的复制图标或右键选择复制表名） | 系统弹出提示「复制成功」，粘贴后内容为「test_table_a」 |

## 数据质量-质量报告

### 质量报告列表页

#### 合理性校验-多表字段值对比规则结果展示

##### 验证质量报告列表页展示合理性校验-多表字段值对比规则校验通过结果 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表中找到包含「合理性校验-多表字段值对比」规则的报告记录，查看该规则行各列内容 | 规则行各列值为：「规则类型」= 合理性校验，「规则名称」= 多表字段值对比校验，「字段名称」= score，「字段类型」= DOUBLE，「质检结果」= · 校验通过，「未通过原因」列为空 |
| 3 | 查看该规则行的「操作」列 | 「操作」列中【查看详情】按钮不可点击或不显示 |

##### 验证质量报告列表页展示合理性校验-多表字段值对比规则校验不通过结果及未通过原因 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表中找到包含「合理性校验-多表字段值对比」规则的报告记录，查看该规则行各列内容 | 规则行各列值为：「规则类型」= 合理性校验，「规则名称」= 多表字段值对比校验，「字段名称」= score，「字段类型」= DOUBLE，「质检结果」= · 校验不通过，「未通过原因」= 字段值计算对比校验未通过 |
| 3 | 查看该规则行的「操作」列 | 「操作」列中【查看详情】按钮处于可点击状态 |

##### 验证质量报告列表页详情说明文案-计算结果值判断-校验通过格式 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表中找到上述规则行，查看「详情说明」列的内容 | 「详情说明」列显示：「符合规则 表a.test_col_2+表b.test_col_3 > 1」，文案前缀为「符合规则」，计算表达式格式与配置的计算逻辑一致 |

##### 验证质量报告列表页详情说明文案-计算结果值判断-校验不通过格式 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表中找到上述规则行，查看「详情说明」列的内容 | 「详情说明」列显示：「不符合规则 表a.test_col_2+表b.test_col_3 > 1」，文案前缀为「不符合规则」，计算表达式格式与配置的计算逻辑一致 |

##### 验证质量报告列表页详情说明文案-计算结果对比-校验通过格式 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表中找到上述规则行，查看「详情说明」列的内容 | 「详情说明」列显示：「符合规则 表a.test_col_2+表b.test_col_3 > 表a.test_col_2+表b.test_col_3」，文案前缀为「符合规则」，格式为两个计算表达式通过运算符连接，与配置的计算逻辑一致 |

##### 验证质量报告列表页详情说明文案-计算结果对比-校验不通过格式 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表中找到上述规则行，查看「详情说明」列的内容 | 「详情说明」列显示：「不符合规则 表a.test_col_2+表b.test_col_3 > 表a.test_col_2+表b.test_col_3」，文案前缀为「不符合规则」，格式为两个计算表达式通过运算符连接，与配置的计算逻辑一致 |

### 质量报告详情弹窗

#### 弹窗字段完整性

##### 验证质量报告详情弹窗展示合理性校验-多表字段值对比规则的完整字段信息 「P0」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表找到上述规则行，点击【查看详情】按钮 | 质量报告详情弹窗打开 |
| 3 | 查看弹窗中的所有字段展示 | 弹窗展示以下字段且值正确：「字段」= score，「统计函数」= （为空或无），「多表字段值对比」= 多表字段值对比，「过滤条件」= score > 0，「校验表主键」= id，「关联表1所属库」= test_db，「关联表1」= test_table_b，「关联表1主键」= ref_id，「对比方法」= 计算值结果判断，「计算逻辑」= 表a.score + 表b.value > 1，「强弱规则」= 强规则，「规则描述」= 校验score字段与关联表value字段之和大于1 |

##### 验证质量报告详情弹窗-计算结果对比模式下字段展示 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表找到上述规则行，点击【查看详情】按钮 | 质量报告详情弹窗打开 |
| 3 | 查看「对比方法」和「计算逻辑」字段的值 | 「对比方法」= 计算结果对比，「计算逻辑」= 表a.test_col_2+表b.test_col_3 > 表a.test_col_2+表b.test_col_3，格式为左侧计算表达式 + 运算符 + 右侧计算表达式；「强弱规则」= 弱规则 |

##### 验证质量报告详情弹窗展示多个关联表的字段信息 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表找到上述规则行，点击【查看详情】按钮 | 质量报告详情弹窗打开 |
| 3 | 查看弹窗中关联表相关字段展示 | 弹窗按关联表编号分别展示：「关联表1所属库」= test_db，「关联表1」= test_table_b，「关联表1主键」= ref_id_b；「关联表2所属库」= test_db2，「关联表2」= test_table_c，「关联表2主键」= ref_id_c；两组关联表字段均完整呈现 |

##### 验证质量报告详情弹窗-校验通过时【查看详情】不可打开弹窗 「P1」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量-质量报告】页面 | 质量报告列表页正常加载 |
| 2 | 在质量报告列表找到「校验通过」状态的「合理性校验-多表字段值对比」规则行，查看「操作」列 | 「操作」列的【查看详情】按钮不可点击或不显示，无法打开详情弹窗 |
