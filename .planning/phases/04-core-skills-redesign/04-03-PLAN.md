---
phase: 04-core-skills-redesign
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - .claude/skills/prd-enhancer/SKILL.md
  - .claude/skills/prd-enhancer/prompts/prd-formalizer.md
  - .claude/skills/code-analysis-report/SKILL.md
  - .claude/skills/code-analysis-report/references/bug-report-template.md
  - .claude/skills/code-analysis-report/references/env-vs-code-checklist.md
autonomous: true
requirements: [SKIL-02, SKIL-03]

must_haves:
  truths:
    - "prd-enhancer Step 0.5 title says '原始平台文本检查（条件步骤）' not 'DTStack 正式需求文档检查'"
    - "prd-enhancer description YAML contains no 'DTStack 特殊说明' paragraph"
    - "prd-enhancer SKILL.md has no DTStack-specific field references in frontmatter examples"
    - "code-analysis-report has Mode C (前端报错分析) in pattern recognition table"
    - "code-analysis-report auto-detects frontend vs backend errors using keyword matching"
    - "code-analysis-report frontend template reuses existing HTML report styles"
  artifacts:
    - path: ".claude/skills/prd-enhancer/SKILL.md"
      provides: "Generalized PRD enhancer with conditional Step 0.5"
    - path: ".claude/skills/code-analysis-report/SKILL.md"
      provides: "Extended code analysis with frontend error support (Mode C)"
      contains: "模式C"
    - path: ".claude/skills/code-analysis-report/references/bug-report-template.md"
      provides: "Frontend error HTML report template section"
  key_links:
    - from: ".claude/skills/code-analysis-report/SKILL.md"
      to: ".claude/skills/code-analysis-report/references/bug-report-template.md"
      via: "Mode C references frontend template"
      pattern: "第四章"
---

<objective>
Generalize prd-enhancer (remove DTStack wording, conditionalize Step 0.5) and extend code-analysis-report with frontend error analysis mode (Mode C).

Purpose: prd-enhancer is the PRD preprocessing layer used by test-case-generator. code-analysis-report is independent but needs frontend support per D-06/D-07/D-08.
Output: Generalized prd-enhancer SKILL.md + new Mode C in code-analysis-report + frontend HTML template
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-skills-redesign/04-CONTEXT.md
@.planning/phases/04-core-skills-redesign/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize prd-enhancer SKILL.md + prd-formalizer.md</name>
  <files>.claude/skills/prd-enhancer/SKILL.md, .claude/skills/prd-enhancer/prompts/prd-formalizer.md</files>
  <read_first>
    - .claude/skills/prd-enhancer/SKILL.md (full file — Step 0.5 DTStack wording, description YAML, frontmatter examples)
    - .claude/skills/prd-enhancer/prompts/prd-formalizer.md (check for DTStack-specific instructions)
    - .claude/rules/archive-format.md (front-matter field names for consistent examples)
    - .planning/phases/04-core-skills-redesign/04-RESEARCH.md (Pattern 3: prd-enhancer Step 0.5 conditionalization example)
  </read_first>
  <action>
    **Edit `.claude/skills/prd-enhancer/SKILL.md`:**

    1. **description YAML (line ~3):** Remove the `> DTStack 特殊说明：...` paragraph entirely (around line 12). This is the paragraph starting with "DTStack 特殊说明" that instructs about prd-formalizer for DTStack.

    2. **Replace it with a generic conditional note:**
       ```markdown
       > 当 config.repos 非空且输入来自外部平台原始文本（如蓝湖 URL 提取内容）时，应先经过 `prd-formalizer` 生成正式需求文档整理结果，再由本 Skill 做图片增强、结构标准化与健康度预检。当 config.repos 为空时，直接对输入文档执行增强。
       ```

    3. **Step 0.5 title (line ~29):** Change `Step 0.5: DTStack 正式需求文档检查` to `Step 0.5: 原始平台文本检查（条件步骤）`

    4. **Step 0.5 body:** Replace DTStack-specific wording with:
       ```markdown
       ## 二点五、Step 0.5: 原始平台文本检查（条件步骤）

       > 仅在 config.repos 非空且输入明显来自外部平台原始文本（如蓝湖 URL 提取内容）时执行。
       > **若 config.repos 为空，跳过本步骤直接进入 Step 1。**

       若 config.repos 非空且内容为原始平台文本：
       1. 检查是否已有 prd-formalizer 整理结果
       2. 若没有，先回退到 formalizer 流程（结合 source_context 源码）
       3. 本 Skill 只对正式需求文档执行后续增强
       ```

    5. **Step 8 frontmatter examples** (if they contain DTStack product field references like specific module names): Replace with generic examples using `${module_key}` and `${version}` placeholders.

    6. **Any other DTStack references:** Search entire file for `DTStack`, `禅道`, `数据资产`, `离线开发`, `数据质量` — replace each with generic equivalents.

    **Read and evaluate `.claude/skills/prd-enhancer/prompts/prd-formalizer.md`:**
    - If it contains DTStack-specific instructions (specific product names, module references), replace with generic `config.repos` conditional wording
    - If it's already generic, leave unchanged and note in summary
  </action>
  <verify>
    <automated>grep -c "DTStack\|禅道\|数据资产\|离线开发\|数据质量" .claude/skills/prd-enhancer/SKILL.md | grep -q '^0$' && echo "PASS" || echo "FAIL: DTStack refs remain"</automated>
  </verify>
  <done>prd-enhancer SKILL.md has zero DTStack references, Step 0.5 uses conditional wording with config.repos guard</done>
  <acceptance_criteria>
    - grep -c 'DTStack' .claude/skills/prd-enhancer/SKILL.md returns 0
    - .claude/skills/prd-enhancer/SKILL.md contains '原始平台文本检查（条件步骤）'
    - .claude/skills/prd-enhancer/SKILL.md contains 'config.repos 非空' (conditional guard)
    - .claude/skills/prd-enhancer/SKILL.md does NOT contain 'DTStack 特殊说明'
    - .claude/skills/prd-enhancer/SKILL.md does NOT contain 'DTStack 正式需求文档检查'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add frontend error analysis Mode C to code-analysis-report</name>
  <files>.claude/skills/code-analysis-report/SKILL.md, .claude/skills/code-analysis-report/references/bug-report-template.md, .claude/skills/code-analysis-report/references/env-vs-code-checklist.md</files>
  <read_first>
    - .claude/skills/code-analysis-report/SKILL.md (full file — current Mode A/B/C structure, Step 2a repo location)
    - .claude/skills/code-analysis-report/references/bug-report-template.md (existing HTML template styles to reuse)
    - .claude/skills/code-analysis-report/references/env-vs-code-checklist.md (existing backend checklist to extend)
    - .claude/skills/code-analysis-report/prompts/code-analyzer.md (check for DTStack-specific content)
    - .planning/phases/04-core-skills-redesign/04-RESEARCH.md (Pattern 4: frontend error mode, keyword table, config.stackTrace extension)
  </read_first>
  <action>
    **Edit `.claude/skills/code-analysis-report/SKILL.md`:**

    1. **Section "一、模式识别" table:** REPLACE current Mode C (信息不足) with Mode D, and ADD new Mode C (前端报错):
       ```markdown
       | 模式 | 触发信号 | 执行路径 |
       |------|---------|----------|
       | **模式A：后端 Bug 分析** | 含 `Exception`/`Caused by`/`at com.`/`java.lang` | → 执行第二章 |
       | **模式B：合并冲突** | 含 `<<<<<<< HEAD`/`=======`/`>>>>>>>` | → 执行第三章 |
       | **模式C：前端报错分析** | 含 `TypeError`/`ReferenceError`/`[Vue warn]`/`React`/`Uncaught Error`/`error TS`/`Hydration failed` | → 执行第四章（新增） |
       | **模式D：信息不足** | 描述模糊，缺少日志或冲突内容 | → 告知用户需补充的材料 |
       ```

    2. **description YAML (line ~3):** Update to mention frontend support:
       Change `当用户粘贴后端报错日志` to `当用户粘贴前端或后端报错日志`
       Remove `专为禅道富文本编辑器粘贴使用` — replace with `输出精美 HTML 格式报告`

    3. **Section "二、Bug 分析模式" — Step 2a 仓库定位:** Add frontend path support to config.stackTrace description. After the existing backend repo location logic, add:
       ```markdown
       **前端报错的仓库定位（模式 C）：**
       - 优先匹配 `config.stackTrace` 中的前端键（如 `@/components`、`react-app`、`vue-app`）
       - Fallback：扫描 `.repos/` 下含 `package.json` 的目录
       - 若无 `.repos/` 配置，跳过源码定位步骤，仅基于报错信息分析
       ```

    4. **ADD new "第四章：前端报错分析模式（模式C）"** at the end of the file (before any appendix):
       ```markdown
       ## 四、前端报错分析模式（模式C）完整流程

       ### Step 1：报错类型识别

       | 报错特征关键词 | 判定 |
       |--------------|------|
       | `TypeError: Cannot read` / `ReferenceError` / `Uncaught Error` | JavaScript 运行时错误 |
       | `Warning: ` / `React.createElement` / `React Hook` / `at Object.<anonymous>` | React 框架错误 |
       | `[Vue warn]` / `VueComponent` | Vue 框架错误 |
       | `Error: Hydration failed` / `getServerSideProps` / `getStaticProps` | Next.js SSR 错误 |
       | `error TS` / `Cannot find module` (无 Java 包名) | TypeScript 编译错误 |

       ### Step 2：仓库定位（条件步骤）

       > 仅在 config.repos 非空时执行。

       1. 从报错堆栈中提取文件路径或组件名
       2. 匹配 config.stackTrace 中的前端键（如 `@/components` → `.repos/frontend/src/components/`）
       3. 若匹配成功，定位到具体源码文件
       4. 若无匹配，提示用户补充仓库信息或跳过源码定位

       ### Step 3：根因分析

       按以下维度分析前端报错：

       1. **组件层**：渲染错误、props 类型不匹配、state 管理异常
       2. **数据层**：API 返回数据结构变更、空值未处理、类型转换失败
       3. **环境层**：Node 版本不兼容、依赖版本冲突、构建配置错误
       4. **框架层**：SSR/CSR hydration 不一致、路由配置错误、中间件异常

       ### Step 4：生成 HTML 报告

       复用 `references/bug-report-template.md` 中的样式，报告结构：

       1. **报错摘要**：错误类型 + 关键信息
       2. **组件堆栈**：从报错中提取的组件调用链（替代后端的 Java 堆栈）
       3. **根因分析**：按上述 4 个维度
       4. **修复建议**：具体代码修改方向
       5. **环境信息**：浏览器/Node 版本、框架版本（从报错中提取）

       报告存放路径：`reports/bugs/` + 日期子目录，刷新 `latest-bug-report.html`。
       ```

    **Edit `.claude/skills/code-analysis-report/references/bug-report-template.md`:**

    5. ADD a frontend report section after the existing backend template. Use the same inline CSS styles but with frontend-specific fields:
       ```html
       <!-- 前端报错报告模板 -->
       <h2>前端报错分析报告</h2>
       <table>
         <tr><th>错误类型</th><td>{{error_type}}</td></tr>
         <tr><th>框架</th><td>{{framework}}</td></tr>
         <tr><th>组件路径</th><td>{{component_path}}</td></tr>
         <tr><th>浏览器/环境</th><td>{{browser_env}}</td></tr>
       </table>
       <h3>组件堆栈</h3>
       <pre>{{component_stack}}</pre>
       <h3>根因分析</h3>
       {{root_cause_analysis}}
       <h3>修复建议</h3>
       {{fix_suggestions}}
       ```

    **Edit `.claude/skills/code-analysis-report/references/env-vs-code-checklist.md`:**

    6. ADD frontend-specific environment vs code checklist items:
       ```markdown
       ## 前端报错判断规则

       | 现象 | 环境问题 | 代码问题 |
       |------|---------|---------|
       | `Cannot find module` | Node 版本或依赖未安装 | import 路径错误 |
       | `Hydration failed` | SSR/CSR 环境不一致 | 组件中使用了 browser-only API |
       | `TypeError: X is not a function` | 依赖版本不兼容 | API 调用方式错误 |
       | 仅特定浏览器报错 | 浏览器兼容性 | 使用了非标准 API |
       | 构建成功但运行报错 | 环境变量缺失 | 运行时类型错误 |
       ```

    **Also check and clean DTStack references:**
    7. In SKILL.md: Remove `专为禅道富文本编辑器粘贴使用` and any other 禅道-specific wording
    8. In prompts/code-analyzer.md: Check for DTStack references and generalize if found
  </action>
  <verify>
    <automated>grep -q '模式C：前端报错分析' .claude/skills/code-analysis-report/SKILL.md && grep -q '第四章' .claude/skills/code-analysis-report/SKILL.md && grep -q '前端报错' .claude/skills/code-analysis-report/references/bug-report-template.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>code-analysis-report has Mode C for frontend errors with keyword detection, frontend HTML template, and frontend env-vs-code checklist; no 禅道-specific wording remains</done>
  <acceptance_criteria>
    - .claude/skills/code-analysis-report/SKILL.md contains '模式C：前端报错分析'
    - .claude/skills/code-analysis-report/SKILL.md contains '第四章：前端报错分析模式'
    - .claude/skills/code-analysis-report/SKILL.md contains 'TypeError' and 'Vue warn' and 'React' in mode detection table
    - .claude/skills/code-analysis-report/references/bug-report-template.md contains '前端报错分析报告' or '前端报错'
    - .claude/skills/code-analysis-report/references/env-vs-code-checklist.md contains '前端报错判断规则'
    - grep -c '禅道' .claude/skills/code-analysis-report/SKILL.md returns 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- prd-enhancer: `grep -rn 'DTStack' .claude/skills/prd-enhancer/SKILL.md` returns empty
- code-analysis-report: Mode C section exists with frontend keyword detection table
- Frontend HTML template exists in bug-report-template.md
- `node --test .claude/tests/` still passes (no script changes in this plan)
</verification>

<success_criteria>
- prd-enhancer works as conditional skill with no DTStack coupling
- code-analysis-report auto-routes frontend errors to Mode C with proper analysis flow
- All existing functionality preserved (Mode A/B unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-skills-redesign/04-03-SUMMARY.md`
</output>
