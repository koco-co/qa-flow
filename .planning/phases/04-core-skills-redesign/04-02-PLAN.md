---
phase: 04-core-skills-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/archive-converter/SKILL.md
  - .claude/skills/archive-converter/scripts/json-to-archive-md.mjs
  - .claude/skills/archive-converter/scripts/convert-history-cases.mjs
  - .claude/tests/test_convert-history-cases_module-map.mjs
autonomous: true
requirements: [SKIL-05]

must_haves:
  truths:
    - "archive-converter SKILL.md output directory table uses config-driven paths, not DTStack/XYZH hardcoded paths"
    - "json-to-archive-md.mjs resolves output directory via config.modules, not DTSTACK_MODULE_MAP or hardcoded '信永中和' check"
    - "convert-history-cases.mjs builds module map dynamically from config.modules instead of hardcoded array"
    - "All DTStack product names removed from archive-converter SKILL.md"
  artifacts:
    - path: ".claude/skills/archive-converter/SKILL.md"
      provides: "Generalized archive converter skill definition"
    - path: ".claude/skills/archive-converter/scripts/json-to-archive-md.mjs"
      provides: "Config-driven archive output path resolution"
    - path: ".claude/skills/archive-converter/scripts/convert-history-cases.mjs"
      provides: "Config-driven module mapping for history case conversion"
    - path: ".claude/tests/test_convert-history-cases_module-map.mjs"
      provides: "Unit tests for dynamic module map building"
  key_links:
    - from: ".claude/skills/archive-converter/scripts/json-to-archive-md.mjs"
      to: ".claude/shared/scripts/load-config.mjs"
      via: "import { loadConfig, resolveModulePath }"
      pattern: "resolveModulePath"
---

<objective>
Generalize archive-converter: replace DTStack/XYZH hardcoded module maps and output paths with config-driven resolution in both SKILL.md and scripts.

Purpose: archive-converter is the second output layer skill. It must use config.modules for all module name mapping and output directory resolution.
Output: Generalized SKILL.md + refactored scripts + unit tests for module map
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-skills-redesign/04-CONTEXT.md
@.planning/phases/04-core-skills-redesign/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — module map dynamic building + script refactor</name>
  <files>.claude/tests/test_convert-history-cases_module-map.mjs, .claude/skills/archive-converter/scripts/convert-history-cases.mjs, .claude/skills/archive-converter/scripts/json-to-archive-md.mjs</files>
  <read_first>
    - .claude/skills/archive-converter/scripts/convert-history-cases.mjs (full file — hardcoded MODULE_MAP, CSV_DIRS, xmind scan paths)
    - .claude/skills/archive-converter/scripts/json-to-archive-md.mjs (full file — DTSTACK_MODULE_MAP, resolveOutputDir with '信永中和' check)
    - .claude/shared/scripts/load-config.mjs (loadConfig, loadConfigFromPath, resetConfigCache, resolveModulePath, getModuleMap APIs)
    - .claude/tests/test_output-naming-contracts.mjs (test isolation pattern with temp config files)
    - .planning/phases/04-core-skills-redesign/04-RESEARCH.md (Pattern 2: convert-history-cases.mjs refactor code example)
  </read_first>
  <behavior>
    - Test 1: buildModuleMap() with config.modules = {orders: {zh: '订单管理'}, products: {zh: '商品管理'}} returns map where '订单管理' -> 'orders' and 'orders' -> 'orders' and '商品管理' -> 'products'
    - Test 2: buildModuleMap() with empty config.modules = {} returns empty object
    - Test 3: resolveOutputDir(meta) with module_key='orders' and version='v2.0' uses resolveModulePath to produce correct archive path
  </behavior>
  <action>
    **Phase RED:** Create `.claude/tests/test_convert-history-cases_module-map.mjs` with the test cases above. Use loadConfigFromPath with temp config for isolation.

    **Phase GREEN — convert-history-cases.mjs refactor:**
    1. REPLACE the hardcoded `MODULE_MAP` initialization. Currently uses `getModuleMap()` from load-config.mjs — verify this is already config-driven. If so, the map building is already generalized; focus on removing hardcoded CSV_DIRS.
    2. REMOVE or CONDITIONALIZE the hardcoded `CSV_DIRS` array (lines ~558-568) that references `cases/history/xyzh/v0.2.0` and `cases/history/xyzh/v0.2.1`. Replace with dynamic scanning:
       ```javascript
       function buildCsvDirs(config) {
         const dirs = []
         const historyBase = join(ROOT, config.casesRoot || 'cases/', 'history')
         // Scan history directory for module subdirectories
         if (existsSync(historyBase)) {
           for (const entry of readdirSync(historyBase, { withFileTypes: true })) {
             if (entry.isDirectory()) {
               const moduleDir = join(historyBase, entry.name)
               // Scan for version subdirectories
               for (const sub of readdirSync(moduleDir, { withFileTypes: true })) {
                 if (sub.isDirectory()) {
                   dirs.push({ dir: join(moduleDir, sub.name), type: 'csv', module: entry.name })
                 }
               }
             }
           }
         }
         return dirs
       }
       ```
    3. REMOVE the hardcoded string `"信永中和"` from the module array (line ~70)
    4. REPLACE the hardcoded XMind scan path `cases/xmind/custom/xyzh/` logic (line ~532-544) with config-driven module scanning using `resolveModulePath(moduleKey, 'xmind', config)`

    **Phase GREEN — json-to-archive-md.mjs refactor:**
    1. RENAME `DTSTACK_MODULE_MAP` to `MODULE_MAP` (remove DTStack prefix)
    2. REMOVE the `projectName === "信永中和"` special case (line ~488-489). Replace with:
       ```javascript
       // Use resolveModulePath for all modules uniformly
       const moduleKey = resolveModuleKey(meta)
       if (moduleKey) {
         return resolveModulePath(moduleKey, 'archive', config, version)
       }
       ```
    3. REMOVE `DTSTACK_MODULES` variable (line ~452)
    4. REPLACE `DTSTACK_MODULE_MAP` references (lines ~466-467, ~477) with the renamed `MODULE_MAP`
    5. Import `resolveModulePath` from load-config.mjs if not already imported

    Export `buildModuleMap` (or equivalent) for testing.

    Run: `node --test .claude/tests/test_convert-history-cases_module-map.mjs && node --test .claude/tests/`
  </action>
  <verify>
    <automated>node --test .claude/tests/test_convert-history-cases_module-map.mjs && node --test .claude/tests/</automated>
  </verify>
  <done>Module map is config-driven, no hardcoded DTStack/XYZH paths in scripts, tests pass</done>
  <acceptance_criteria>
    - .claude/skills/archive-converter/scripts/json-to-archive-md.mjs does NOT contain 'DTSTACK_MODULE_MAP'
    - .claude/skills/archive-converter/scripts/json-to-archive-md.mjs does NOT contain string '信永中和'
    - .claude/skills/archive-converter/scripts/convert-history-cases.mjs does NOT contain hardcoded 'cases/history/xyzh'
    - .claude/tests/test_convert-history-cases_module-map.mjs contains at least 3 'test(' calls
    - `node --test .claude/tests/` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generalize archive-converter SKILL.md — remove DTStack/XYZH tables and examples</name>
  <files>.claude/skills/archive-converter/SKILL.md</files>
  <read_first>
    - .claude/skills/archive-converter/SKILL.md (full file — DTStack module table in section 二 and 六)
    - .claude/rules/archive-format.md (already generalized archive format — reference for consistent wording)
  </read_first>
  <action>
    Edit `.claude/skills/archive-converter/SKILL.md` with the following specific changes:

    1. **Section "一、支持的输入格式" (line ~16-19):** Replace `cases/history/xyzh/v0.x.x/*.csv` with generic `cases/history/${module_key}/${version}/*.csv`

    2. **Section "二、输出目标" table (lines ~28-33):** REPLACE the DTStack/XYZH specific table with:
       ```markdown
       | 来源 | 输出目录 |
       | --- | --- |
       | 模块 XMind | `cases/archive/${module_key}/` |
       | 带版本的模块 XMind | `cases/archive/${module_key}/v${version}/` |
       | 模块 CSV | `cases/archive/${module_key}/${version}/` |

       路径解析使用 `resolveModulePath(moduleKey, 'archive', config, version)`，模块 key 和版本从输入文件路径或 config.modules 推断。
       ```

    3. **Section "六、模块名称映射表"** (if exists): REPLACE hardcoded mapping table with:
       ```markdown
       ## 六、模块名称映射

       模块名称映射从 `config.modules` 动态构建：
       - `config.modules[key].zh` → key（中文名到 key 的映射）
       - key → key（直通映射）

       无需手动维护映射表，添加新模块只需在 config.json 的 modules 中配置。
       ```

    4. **description YAML:** Remove any DTStack/XYZH references from the description field.

    5. **Anywhere else:** Search and replace any remaining `信永中和`, `DTStack`, `xyzh`, `batch-works`, `data-assets` references with generic `${module_key}` templates.
  </action>
  <verify>
    <automated>grep -c "DTStack\|信永中和\|batch-works\|data-assets\|xyzh" .claude/skills/archive-converter/SKILL.md | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>archive-converter SKILL.md contains zero DTStack/XYZH references, all tables use config-driven generic examples</done>
  <acceptance_criteria>
    - grep -c 'DTStack' .claude/skills/archive-converter/SKILL.md returns 0
    - grep -c '信永中和' .claude/skills/archive-converter/SKILL.md returns 0
    - grep -c 'xyzh' .claude/skills/archive-converter/SKILL.md returns 0
    - .claude/skills/archive-converter/SKILL.md contains 'resolveModulePath' or '${module_key}'
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test .claude/tests/` exits 0
- `grep -rn 'DTStack\|信永中和\|DTSTACK_MODULE_MAP' .claude/skills/archive-converter/` returns empty (excluding comments explaining migration)
- Scripts use config.modules for all module resolution
</verification>

<success_criteria>
- archive-converter works with any project's config.modules without hardcoded business data
- Module map is dynamically built from config, supporting arbitrary project module names
- Full test suite green including new module map tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-skills-redesign/04-02-SUMMARY.md`
</output>
