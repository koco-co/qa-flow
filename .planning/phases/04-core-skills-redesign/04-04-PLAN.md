---
phase: 04-core-skills-redesign
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .claude/skills/test-case-generator/SKILL.md
  - .claude/skills/test-case-generator/prompts/writer-subagent.md
  - .claude/skills/test-case-generator/prompts/writer-subagent-reference.md
  - .claude/skills/test-case-generator/prompts/step-brainstorm.md
  - .claude/skills/test-case-generator/prompts/step-checklist.md
  - .claude/skills/test-case-generator/prompts/reviewer-subagent.md
  - .claude/skills/test-case-generator/prompts/step-source-sync.md
  - .claude/skills/test-case-generator/prompts/step-prd-formalize.md
  - .claude/skills/test-case-generator/references/elicitation-dimensions.md
autonomous: true
requirements: [SKIL-01]

must_haves:
  truths:
    - "Writer subagent examples use e-commerce scenario (商品管理、订单处理、用户中心) not DTStack business data"
    - "Writer-subagent-reference.md examples are consistent with writer-subagent.md e-commerce scenario"
    - "source-sync step has correct conditional skip logic: config.repos empty -> skip + update last_completed_step"
    - "prd-formalize step has correct conditional skip logic: config.repos empty -> skip"
    - "SKILL.md quick mode table references generic chapter names, not 'DTStack 额外要求'"
    - "SQL precondition examples use ${datasource_type}/${schema}/${table} variable template style"
  artifacts:
    - path: ".claude/skills/test-case-generator/prompts/writer-subagent.md"
      provides: "E-commerce example data for Writer subagent"
      contains: "商品名称"
    - path: ".claude/skills/test-case-generator/prompts/writer-subagent-reference.md"
      provides: "Consistent e-commerce reference examples"
      contains: "订单"
    - path: ".claude/skills/test-case-generator/SKILL.md"
      provides: "Generalized test case generator orchestration"
  key_links:
    - from: ".claude/skills/test-case-generator/SKILL.md"
      to: ".claude/skills/test-case-generator/prompts/writer-subagent.md"
      via: "Step 7 writer invocation"
      pattern: "writer-subagent"
    - from: ".claude/skills/test-case-generator/prompts/writer-subagent.md"
      to: ".claude/skills/test-case-generator/prompts/writer-subagent-reference.md"
      via: "Reference loading"
      pattern: "writer-subagent-reference"
---

<objective>
Generalize test-case-generator: replace all DTStack examples with unified e-commerce scenario in Writer/Reviewer prompts, add conditional skip logic to source-sync and prd-formalize steps, clean up SKILL.md references.

Purpose: test-case-generator is the main orchestration skill that calls all other skills. It has the largest volume of DTStack-specific example data in Writer/Reviewer prompts.
Output: Fully generalized prompts with e-commerce examples, conditional step logic, clean SKILL.md
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-skills-redesign/04-CONTEXT.md
@.planning/phases/04-core-skills-redesign/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace DTStack examples in Writer/Reviewer prompts with e-commerce scenario</name>
  <files>.claude/skills/test-case-generator/prompts/writer-subagent.md, .claude/skills/test-case-generator/prompts/writer-subagent-reference.md, .claude/skills/test-case-generator/prompts/reviewer-subagent.md</files>
  <read_first>
    - .claude/skills/test-case-generator/prompts/writer-subagent.md (full file — all DTStack example data, the largest change target)
    - .claude/skills/test-case-generator/prompts/writer-subagent-reference.md (full file — must stay consistent with writer-subagent.md)
    - .claude/skills/test-case-generator/prompts/reviewer-subagent.md (full file — check for DTStack example data)
    - .claude/rules/test-case-writing.md (already generalized writing rules — reference for example style)
    - .planning/phases/04-core-skills-redesign/04-RESEARCH.md (Pattern 3: e-commerce scenario definition and SQL template)
  </read_first>
  <action>
    **Edit `.claude/skills/test-case-generator/prompts/writer-subagent.md`:**

    This is the largest change. Replace ALL DTStack business examples with the unified e-commerce scenario defined in D-03:

    1. **Example test case steps:** Replace DTStack-specific examples like:
       - `在「问题名称」输入框输入"2026年3月产线温度异常"` → `在「商品名称」输入框输入"2026春季新款运动鞋"`
       - `在「处理状态」下拉框选择"已完成"` → `在「状态」下拉框选择"上架"`
       - `在「问题分类」选择"质量缺陷"` → `在「商品分类」下拉框选择"运动鞋"`

    2. **Example pages/modules:** Replace DTStack module examples:
       - `质量问题台账` → `商品管理`
       - `数据质量` → `商品管理`
       - `规则集配置` → `订单处理`
       - `数据资产` → `用户中心`
       - Navigation examples: `进入【数据质量 → 通用配置 → JSON格式配置】页面` → `进入【商品管理 → 商品列表】页面`

    3. **Example form fields:** Use e-commerce form fields:
       - 商品管理表单：商品名称（文本）、商品分类（下拉）、SKU编号（文本）、价格（数值）、库存数量（数值）、状态（上架/下架/待审核）
       - 订单处理表单：订单号（自动生成）、客户姓名、商品列表、总金额、支付方式（微信/支付宝/余额）、订单状态
       - 用户中心表单：用户名、手机号、邮箱

    4. **SQL precondition examples (D-05):** Replace with variable template style:
       ```
       1、${datasource_type} 测试数据准备:
       DROP TABLE IF EXISTS ${schema}.${table};
       CREATE TABLE ${schema}.${table} (
         id INT,
         product_name VARCHAR(100),
         category VARCHAR(50),
         price DECIMAL(10,2),
         stock_qty INT,
         status VARCHAR(20),
         create_time DATETIME
       );
       INSERT INTO ${schema}.${table} VALUES
         (1, '2026春季新款运动鞋', '运动鞋', 299.00, 100, '上架', '2026-01-01 10:00:00'),
         (2, '经典白色帆布鞋', '帆布鞋', 199.00, 50, '下架', '2026-01-02 10:00:00');
       ```

    5. **Expected results examples:** Replace with e-commerce:
       - `页面提示"新增成功"，列表页新增一条记录，问题名称显示为"原料批次不合格"` → `页面提示"新增成功"，列表页新增一条记录，商品名称显示为"2026春季新款运动鞋"`

    6. **Any "DTStack 额外要求" chapter title:** Rename to `源码分析参考`（通用名称）or remove the DTStack prefix entirely

    **Edit `.claude/skills/test-case-generator/prompts/writer-subagent-reference.md`:**

    7. Synchronize ALL example data with writer-subagent.md changes above. Every example in this file must use the same e-commerce scenario. Pay special attention to:
       - Example case titles
       - Example form field names
       - Example navigation paths
       - Any chapter titled "DTStack 额外要求" → rename to generic

    **Edit `.claude/skills/test-case-generator/prompts/reviewer-subagent.md`:**

    8. Check for DTStack example data. If found, replace with e-commerce equivalents. The reviewer examples should reference the same scenario (商品管理、订单处理) so Writer and Reviewer outputs are consistent.
  </action>
  <verify>
    <automated>grep -c "DTStack\|数据资产\|离线开发\|质量问题台账\|数据质量\|规则集\|禅道\|信永中和" .claude/skills/test-case-generator/prompts/writer-subagent.md .claude/skills/test-case-generator/prompts/writer-subagent-reference.md .claude/skills/test-case-generator/prompts/reviewer-subagent.md | grep -v ':0$' | wc -l | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All Writer/Reviewer prompts use e-commerce examples exclusively, zero DTStack business data references</done>
  <acceptance_criteria>
    - grep -c 'DTStack' .claude/skills/test-case-generator/prompts/writer-subagent.md returns 0
    - grep -c '质量问题台账' .claude/skills/test-case-generator/prompts/writer-subagent.md returns 0
    - .claude/skills/test-case-generator/prompts/writer-subagent.md contains '商品名称' (e-commerce field)
    - .claude/skills/test-case-generator/prompts/writer-subagent.md contains '${datasource_type}' (SQL template)
    - .claude/skills/test-case-generator/prompts/writer-subagent-reference.md contains '商品' or '订单' (e-commerce scenario)
    - grep -c 'DTStack' .claude/skills/test-case-generator/prompts/writer-subagent-reference.md returns 0
    - grep -c 'DTStack' .claude/skills/test-case-generator/prompts/reviewer-subagent.md returns 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generalize SKILL.md + conditional step prompts + remaining prompt files</name>
  <files>.claude/skills/test-case-generator/SKILL.md, .claude/skills/test-case-generator/prompts/step-source-sync.md, .claude/skills/test-case-generator/prompts/step-prd-formalize.md, .claude/skills/test-case-generator/prompts/step-brainstorm.md, .claude/skills/test-case-generator/prompts/step-checklist.md, .claude/skills/test-case-generator/references/elicitation-dimensions.md</files>
  <read_first>
    - .claude/skills/test-case-generator/SKILL.md (full file — quick mode table with 'DTStack 额外要求' reference, description YAML)
    - .claude/skills/test-case-generator/prompts/step-source-sync.md (verify conditional skip logic format)
    - .claude/skills/test-case-generator/prompts/step-prd-formalize.md (verify conditional skip logic format)
    - .claude/skills/test-case-generator/prompts/step-brainstorm.md (check for DTStack examples)
    - .claude/skills/test-case-generator/prompts/step-checklist.md (check for DTStack examples)
    - .claude/skills/test-case-generator/references/elicitation-dimensions.md (check for DTStack examples)
    - .planning/phases/04-core-skills-redesign/04-RESEARCH.md (Pattern 1: conditional step skip format)
  </read_first>
  <action>
    **Edit `.claude/skills/test-case-generator/SKILL.md`:**

    1. **Quick mode table (line ~34):** Change `加载「源码分析」和「DTStack 额外要求」章节` to `加载「源码分析」章节`（remove DTStack reference）

    2. **description YAML (line ~3):** The description mentions 蓝湖 URL — this is a generic feature (蓝湖 is a design tool used across industries), keep it. But check for any DTStack-specific module examples like `data-assets v6.4.10` and replace with generic `${module_key} v${version}`.

    3. **Any routing examples** that reference specific DTStack modules: Replace with generic `${module_key}` or e-commerce examples.

    **Verify conditional step prompts (D-01/D-02):**

    4. **`step-source-sync.md`:** Ensure it has the conditional skip pattern at the top:
       ```markdown
       > 本步骤仅在 config.json 中 `repos` 字段为非空对象时执行。
       > **若 `repos: {}` 则跳过**：
       > 1. 向 execution_log 追加 `{"step": "source-sync", "status": "skipped", "reason": "config.repos is empty"}`
       > 2. 更新 `last_completed_step` 为 `"source-sync"`
       > 3. 继续下一步
       ```
       If this pattern is missing or incomplete, add it. If it's already there from Phase 1, verify the `last_completed_step` update line is present.

    5. **`step-prd-formalize.md`:** Same conditional skip pattern:
       ```markdown
       > 本步骤仅在 config.json 中 `repos` 字段为非空对象时执行。
       > **若 `repos: {}` 则跳过**：
       > 1. 向 execution_log 追加 `{"step": "prd-formalize", "status": "skipped", "reason": "config.repos is empty"}`
       > 2. 更新 `last_completed_step` 为 `"prd-formalize"`
       > 3. 继续下一步
       ```

    **Check remaining prompts for DTStack content:**

    6. **`step-brainstorm.md`:** Read and check for DTStack-specific examples. If found, replace with e-commerce equivalents (商品管理 brainstorming example).

    7. **`step-checklist.md`:** Read and check for DTStack-specific examples. If found, replace with e-commerce equivalents.

    8. **`references/elicitation-dimensions.md`:** Read and check for DTStack-specific examples. If found, replace with generic/e-commerce equivalents.

    9. **Also check** `references/decoupling-heuristics.md` if it exists — verify no DTStack references.
  </action>
  <verify>
    <automated>grep -rl "DTStack\|数据资产\|离线开发\|质量问题台账\|禅道\|信永中和" .claude/skills/test-case-generator/ | wc -l | grep -q '^0$' && echo "PASS: No DTStack refs in test-case-generator" || echo "FAIL"</automated>
  </verify>
  <done>test-case-generator SKILL.md and all prompt/reference files contain zero DTStack references, conditional steps have proper skip + state update logic</done>
  <acceptance_criteria>
    - grep -rl 'DTStack' .claude/skills/test-case-generator/ returns empty
    - grep -c '质量问题台账' .claude/skills/test-case-generator/SKILL.md returns 0
    - .claude/skills/test-case-generator/SKILL.md does NOT contain 'DTStack 额外要求'
    - .claude/skills/test-case-generator/prompts/step-source-sync.md contains 'last_completed_step'
    - .claude/skills/test-case-generator/prompts/step-prd-formalize.md contains 'last_completed_step'
    - `node --test .claude/tests/` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -rl 'DTStack\|数据资产\|离线开发\|质量问题台账\|禅道\|信永中和' .claude/skills/test-case-generator/` returns empty
- Writer and Writer-reference examples are consistent (both use e-commerce)
- Conditional steps (source-sync, prd-formalize) have skip + state update logic
- `node --test .claude/tests/` passes
</verification>

<success_criteria>
- test-case-generator orchestration works for any project with config-driven routing
- E-commerce examples provide concrete, actionable templates for new users
- Conditional steps correctly skip when config.repos is empty, with proper state persistence
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-skills-redesign/04-04-SUMMARY.md`
</output>
